# React + TypeScript GitHub Pages Starter

This project now uses Vite, React, and TypeScript.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

## Deployment

The GitHub Actions workflow publishes the built site to GitHub Pages automatically on pushes to the main branch.
